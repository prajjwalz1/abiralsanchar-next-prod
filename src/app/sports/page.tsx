import { SportsMetadata } from "@/utils/seo/metadatas";
import { Metadata } from "next";

// SEO Metadata for Home
export const metadata: Metadata = SportsMetadata;

const Sports = () => {
  return <div>Sports</div>;
};

export default Sports;
