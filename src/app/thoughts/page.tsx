import { Metadata } from "next";
import { ThoughtsMetadata } from "@/utils/seo/metadatas";

// SEO Metadata for Home
export const metadata: Metadata = ThoughtsMetadata;

const Thoughts = () => {
  return <div>thoughts</div>;
};

export default Thoughts;
